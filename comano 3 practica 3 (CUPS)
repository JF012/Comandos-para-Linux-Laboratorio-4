# Actualiza repositorios
$ 'sudo apt update

# Instala el servicio cups 
$ 'sudo apt install -y cups printer-driver-cups-pdf

# Activa el servicio cups 
$ 'sudo systemctl enable --now cups

# Otorga permiso de usuario
$ 'sudo usermod -a -G lpadmin $USER

# Reconstruye cups 
$ 'sudo nano /etc/cups/cupsd.conf

# Agrega el contenido: 

LogLevel warn
PageLogFormat
MaxLogSize 0
ErrorPolicy retry-job

# Escucha en IPP
Port 631
Listen /run/cups/cups.sock
ServerAlias *
Browsing On
BrowseLocalProtocols dnssd
DefaultAuthType Basic
WebInterface Yes

<Location />
  Order allow,deny
  Allow @LOCAL
</Location>
<Location /admin>
  AuthType Default
  Require user @SYSTEM
  Order allow,deny
  Allow @LOCAL
</Location>
<Location /admin/conf>
  AuthType Default
  Require user @SYSTEM
  Order allow,deny
  Allow @LOCAL
</Location>

<Policy default>
  JobPrivateAccess default
  JobPrivateValues default
  SubscriptionPrivateAccess default
  SubscriptionPrivateValues default
  <Limit Create-Job Print-Job Print-URI Validate-Job>
    Order deny,allow
  </Limit>
  <Limit Send-Document Send-URI Hold-Job Release-Job Restart-Job Purge-Jobs Set-Job-Attributes Create-Job-Subscription Renew-Subscription Cancel-Subscription Get-Notifications Reprocess-Job Cancel-Current-Job Resume-Job Cancel-My-Jobs Close-Job CUPS-Move-Job CUPS-Get-Document>
    AuthType Default
    Require user @OWNER @SYSTEM
    Order deny,allow
  </Limit>
  <Limit CUPS-Add-Modify-Printer CUPS-Delete-Printer CUPS-Add-Modify-Class CUPS-Delete-Class CUPS-Set-Default>
    AuthType Default
    Require user @SYSTEM
    Order deny,allow
  </Limit>
  <Limit Pause-Printer Resume-Printer Enable-Printer Disable-Printer Pause-Printer-After-Current-Job Hold-New-Jobs Release-Held-New-Jobs Deactivate-Printer Activate-Printer Restart-Printer Shutdown-Printer Startup-Printer Promote-Job Schedule-Job-After Cancel-Jobs CUPS-Accept-Jobs CUPS-Reject-Jobs>
    AuthType Default
    Require user @SYSTEM
    Order deny,allow
  </Limit>
  <Limit Cancel-Job CUPS-Authenticate-Job>
    AuthType Default
    Require user @OWNER @SYSTEM
    Order deny,allow
  </Limit>
  <Limit All>
    Order deny,allow
  </Limit>
</Policy>


# Validar y reiniciar
$ 'sudo cupsd -t
$ 'sudo systemctl restart cups.socket
$ 'sudo systemctl restart cups.service

# Busca tu ip
$ 'ip addr show 

# Entonces en tu maquina fisica entra a cups mediante este link: (http://TU_IP:631)

# Crea tu impresora virtual:
- Administration
- Add printer
- CUPS-PDF (VIRTUAL PDF PRINTER) ---> CONTINUE
- Le pones el nombre que quieras Clickea 'Share this printer' ---> CONTINUE
- Generic ---> CONTINUE 
- 'SET PRINTER OPTIONS

# Entra en ajustes de windows
- 'Impresoras y escaneres
- 'Agregar impresoras manualmente
- 'Selecciona impresora compartida por nombre y agrega el link de la impresora (ej: http://TU_IP:631/printers/EL_NOMBRE_DE_TU_IMPRESORA ---> SIGUIENTE
- 'Selecciona Microsoft
- 'Selecciona PS Class Driver ---> OK  ---> SIGUIENTE
- 'Imprimir hoja de prueba ---> OK
- 'Refresca la pagina y podras ver la actividad 

# Otorga permisos de admin a la impresora en linux 
$ 'sudo lpadmin -p PRUEBA_IMPRESION_PDF -o printer-is-shared=true -E lpstat -v PRUEBA_IMPRESION_PDF
 
# Lista en linux las impresiones realizadas 
$ 'ls -lt ~/PDF 2>/dev/null | head
$ 'sudo ls -lt /var/spool/cups-pdf/$USER 2>/dev/null | head
$ 'sudo ls -lt /var/spool/cups-pdf/ANONYMOUS 2>/dev/null | head

# Abre el archivo pdf
$ 'xdg-open "/var/spool/cups-pdf/ANONYMOUS/MSxpsPS__0.pdf" 2>/dev/null || \ xdg-open "/var/spool/cups-pdf/$USER/MSxpsPS__0.pdf"





